---
title: 前端八股
date: 2026-07-03 23:00:38
tags: 前端八股
excerpt: 前端八股
categories: 
    - 面试
---

`编程的本质其实就是：用变量和数据类型在内存里做数学题`

`渴望底层的工程师来说，是有史以来最好的时代`

`scoped`：给当前组件样式加唯一 data-v 前缀，隔离组件样式冲突

`<script setup>` 是编译期语法糖，无需 `export default {}`，简化组合式 API  不写变量只是模块私有，模板无法访问 就是请求数据这时候 比 创造dom实例还早

`<template>` 是**无渲染占位标签**，本身不会生成真实 DOM 节点，只做分组容器。

`{{插值表达式}}`

`Document Object Model`  把html文档变成一棵树 对外提供api 让能让js操作 宿主对象 浏览器解析html形成在内存里的东西 不是js原深对象 虚拟dom是对象，存在内存里面


# 内存 本质链表结构
 
栈内存:基础类型 7种 还有一个地址指正

堆内存:引用类型 

# 组件通信 

props、emit、v-model语法糖（props + emit 封装）

defineProps,defineEmit 编译器宏 只能选择

<h1>props 属性</h1>

父传子

Vue 严格遵循单向数据流（One-Way Downstream Binding）原则。数据只能从父组件流向子组件，子组件无权直接修改它

要修改就是子件给父件发defineEmit

toRefs:进行响应式解构。

const {父组件的变量 } = props ;什么玩意的失去响应性拿toRefs保持响应性

和ts的要的变量的区别

```
组件想复用，配置留接口；
属性叫 Props，数据向下走；
并非 Vue 独创，原生早就有；
框架封装好，响应自动修。
```

# emit 子传父

子组件  触发事件，传数据给父

父组件监听子组件事件，接收数据

# v-model是 props emit 的语法糖


# Proxy 代理 浏览器原生的监听  拦截器（监听器） 数据层 响应式本质

全局监听这个浏览器上的页面是否刷新的存在

Proxy 的监听：它是为了通知页面更新而存在的，是 Vue 3 框架的生命线。get数据 触发刷新

`写时复制`的 Proxy 就是监听神深拷贝的数据变化 今天又在什么地方遇到了没记下来 好像是



# watch （侦听器）

监听 无缓存 通信 数据变化 做异步 / 复杂操作


# Methods 就是页面的“动作触发器”

**普通事件 / 工具函数**，主动触发执行（点击、定时器手动调用） click console.log 什么的

手动触发：点击事件、函数主动调用、页面重渲染强制执行 刷新就执行


# computed（计算属性）

computed 有依赖缓存，依赖不变不会 重复执行刷新页面不会变 只有响应式数据变了

创建 计算属性 ，依赖响应式数据，自带缓存 自动渲染

只有响应式的数据才能计算

A 变了，B 自动跟着变

需要return

计算改变的的数据 pinia里面的state例如

和`Get(取值/读)` 和

写set有屎山 不推荐给确定的computed变量赋值  数据派生可以

当你强行给已经确定的computed变量赋值时候 `Set(赋值/改)` 搭配

# 变量 

| 关键字 | 作用域 | 能否重赋值 | 能否重复声明 | 特性 |
| ---- | ---- | ---- | ---- | ---- |
| var | 函数域还有全局作用域 | ✅ 可改 | ✅ 允许 | 老旧、有坑 |
| let | 块级域 | ✅ 可改 | ❌ 禁止 | 可变变量 |
| const | 块级域 | ❌ 不可重赋本质是 | ❌ 禁止 | 只读常量 |定义常量 值在栈内存里锁死 在给他赋值会报错|

# 常量

const 定义的

cont pi = 1;这个pi就是常量

特点: 值在栈内存里面 不能修改 





# 作用域链和作用域

作用域: 就是函数里面定义一个变量 let var const 变量 在函数内部行程的作用域

一个定义的变量向上查找变量形成的链表 叫做作用域链

```
let x = 1;
function A{
    console.log(x)
}
```

 这个变量x向上找 直到找到这个变量 所形成的链表

当前作用域 + 所有外层父级作用域 逐层向上串联形成的链表，就是作用域链。


# 闭包

变量在词法作用域之外执行

内层函数里的变量向外函数向上找定义的时候形成了作用域链，然后内层函数被return出去了，外面的其他函数用到了外层函数，本应该外层函数执行完入栈出栈会被GC销毁，但因为这个作用域链还存在，这变量没有销毁，为什么是外层函数，应为全局作用域读取不到inner那个`<setup>` 


# 防抖节流
 
settime 计时器 微任务 


# 设计类型 

这为什么玩意？

# 垃圾回收(GC)

什么根节点


# 数据类型
 
引用类型数据（复杂数据类型） 对象、数组、Map、Set、Date日期对象 正则 JS 引用类型存的是`栈内存`   本质都是对象

原始类型（基本数据类型）：数字、字符串、布尔、null、undefined、symbol（简单原始值） 存的是`堆内存`  7种

# 面向对象oop

把凌乱的 变量函数打包
```
// 浏览器给不了你这个，得你自己用面向对象写出来
class ShoppingCart {
    constructor() {
        this.goodsList = []; // 购物车里的商品数据
    }
    addGoods(item) {        // 往购物车加商品的动作
        this.goodsList.push(item);
    }
    calcTotalPrice() {     // 自动算总价的动作
        // 算出总价...
    }
}

// 创造一个你自己的购物车对象
const myCart = new ShoppingCart();
myCart.addGoods({ name: '键盘', price: 299 });

```


# 深\浅拷贝

本质开辟新的堆内存来纯储 深的盒子都变了 浅的盒子不变

copy 一个对象来用 保留原对象不被污染 复用

深就是对象嵌套一个对象你copy了之后原来对象会污染 `{name: 123, test1 :{ tset2:321 } }`

所以出现了深拷贝的方法

1、变为json 穷人版的深拷贝 就是 `parse`  `stringify` `json` 纯文本字符串 打破简直对之间的关系

2、`structuredClone` 结构划拷贝 函数什么的会丢失

3.自己手写递归深拷贝函数
 有点难


实际运用 

让数据回滚

表单编辑场景（最常用） 后端返回原始表单数据，前端你改不能改原始数据 所以出现了拷贝

原列表数据不能被排序 / 过滤破坏，拷贝一份操作：

不想污染接口原始返回对象，临时加字段：

多个组件共用一份接口数据，各自独立修改互不干扰。

# 同步和异步

阻塞 await

异步不会阻塞 async

 await async的底层就是promise的语法糖  简化了.then 链式调用，让异步代码看起来像同步。

`.then` 接收成功结果，`.catch` 捕获错误，`.finally` 无论成败都会执行

# v-if 和 v-show

v-if ="强制布尔值转换 `0 ``空的字符串` false  什么乱七八糟的

v-show 真实dom 本质`display= "none"`

到底 发生了什么也想知道

# 隐藏标签？元素的方法？

`v-if / v-else-if / v-else` dom销毁

`v-show` dom纯在 display:none;

`display 隐藏（同 v-show 逻辑）`

`visibility 透明隐藏（保留占位) 不脱里文档流 不能点击`

`opacity 完全透明（元素还在，可点击`

`z-index 覆盖`

`给屏幕阅读器专用` 这什么玩意没接触过

# grid 和 flex 布局 居中

块级block：独占一行，可设宽高，默认 100% 宽 div p h1 ui li ... `margin flex:just-center= center aline-height= center` `grid:place-item = center`

行内inline：同行展示，宽高无效，仅左右边距生效  span a i b ... `line-height= height`

行内块inline-block：同行展示，可设宽高，存在文字间隙 这什么玩意怎么 为什么没用过

# 设计模式 23种

前人写前端通用问题的**标准套路**，不用自己瞎写重复逻辑。

**解决一类重复麻烦问题的固定思路、代码套路**。

怎么和这个代理proxy扯上关系的

```
1. Vue2：**观察者模式**（响应式）
2. Vue3：**代理模式 Proxy**
3. 组件通信 EventBus：**发布订阅**
4. React HOC / Hooks：**装饰器模式**
5. Pinia/Vuex 全局状态：**单例模式**
6. 表单校验、多业务分支：**策略模式**
7. 弹窗组件：**单例模式**
```

# 本地存储 localStorage






