---
title: 'mysql里面text类型放markdown格式'
date: 2026-05-25 18:27:50
tags: markdown格式
excerpt: 长文本数据在数据库里面markdown格式
categories:
    - MySQL
---

# 直接存 Markdown 格式（现在全行业最流行）
怎么渲染？ 前端有很多现成的插件（比如 marked.js、md-editor-v3），丢给它们，一瞬间就能渲染出极其漂亮的排版，根本不需要程序员自己去解析。


# 直接存 JSON 数组（如果你非要前端强行把每一段拆开）
如果你因为某种特殊的业务需求（比如你想给每一段加上不同的动效），必须让前端拿到一个“数组”，那你在存入数据库之前，就在后端（或前端输入时）把它转成 JSON 字符串存进去：

怎么渲染？ 前端拿到后直接 JSON.parse() 解开，就是一个现成的数组，直接用 v-for 循环渲染就行了。